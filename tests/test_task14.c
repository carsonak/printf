#include "tests.h"

/**
 * test - TASK14 tests.
 *
 * Return: 0 if all tests pass, 1 otherwise.
 */
bool test(void)
{
	/* int len__printf = 0, len_sprintf = 0; */
	bool failed = 0;

	return (failed);
}
